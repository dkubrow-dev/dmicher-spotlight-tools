// Browser path between sibling Foundry modules; tests map the repository layout.
import { requireApiVersion } from "../../dmicher-generics/scripts/api.js";

export const generics = requireApiVersion(1);
